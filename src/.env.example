# copy ไฟล์นี้เป็น src/.env (git ไม่เก็บ .env — ธรรมเนียมเดียวกับ server/.env.example)
# เว้นว่างไว้ = เรียก API แบบ relative ผ่าน Vite proxy (ดู vite.config.js)
# ทำให้เครื่องอื่นในวง LAN เข้าใช้ผ่าน http://IP:5173 ได้โดยไม่ติด CORS
VITE_API_URL=
